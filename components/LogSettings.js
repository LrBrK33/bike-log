import React, {useState} from 'react';
import {View, Text, StyleSheet, TouchableOpacity} from 'react-native';

const LogSettings = () => {
  return (
    <View>
      <Text>Log Settings here</Text>
    </View>
  );
};

export default LogSettings;
